# .env.example — Plantilla pública para docker-compose.yml.
# Copia este archivo a ".env" (mismo nivel) y ajusta los valores.
#   cp .env.example .env
# NO confundir con backend/.env.example (ese es para la app FastAPI).

POSTGRES_USER=postgres
POSTGRES_PASSWORD=cambia_esta_password
POSTGRES_DB=BD_GRIN
