# requirements.txt — Dependencias DIRECTAS (curadas, multiplataforma).
# Úsalo para desarrollo y para deploys donde quieras que pip resuelva las
# transitivas según el SO (ej. uvloop en Linux). Para un deploy EXACTO/bit-a-bit
# usa requirements.lock.txt (freeze completo). Si agregas/actualizas una dep aquí,
# regenera el lock:  pip install -r requirements.txt && pip freeze > requirements.lock.txt
# (regenéralo en el mismo SO de destino).

fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1
reportlab==4.2.0
pandas==2.2.2
openpyxl==3.1.2
pypdf==4.2.0
python-multipart==0.0.9
xhtml2pdf==0.2.16
pillow==12.2.0
# Cola de tareas pesadas (infra async compartida — módulo LIST y futuros)
redis==5.0.4
rq==1.16.2
